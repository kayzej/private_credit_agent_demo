"""Project MCP server: exposes ingest/extract/engine capabilities as MCP tools and resources.

Package is named ``mcp_server`` (not ``mcp``) so it does not shadow the upstream
``mcp`` PyPI package (the official Model Context Protocol SDK) once that is added
as a dependency.
"""
